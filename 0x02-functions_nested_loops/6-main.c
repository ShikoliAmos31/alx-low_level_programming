#include "main.h"

/**
* _abs - program that finds the absolute value of an integer
* @a: parameter for finding an integer
* Return: absolute value of a
*/
int _abs(int a)
{
if (a < 0)
return -a;

return a;
}
